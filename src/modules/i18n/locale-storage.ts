/**
 * Key used with localStorage to remember manual language choice.
 * Must match the literal used in `src/pages/index.astro` redirect script.
 */
export const preferredLocaleStorageKey = 'portfolio-locale';

